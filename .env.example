# Docker Compose liest diese Datei automatisch ein.
# Vor dem ersten Start nach .env kopieren und JWT_SECRET mit einem
# zufälligen Wert füllen, z. B.:
#   openssl rand -base64 32
# Die .env-Datei ist in .gitignore ausgeschlossen und wird nie committet.
JWT_SECRET=
